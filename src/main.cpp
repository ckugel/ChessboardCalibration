#include <opencv2/core.hh>
#include <iostream>

using namespace std;

int main() {
    string im1path;
    string im2path;

    Mat im1 = cv::imread(im1path);
    Mat im2 = cv::imread(im2path);
    
    
    
    return -1;
}